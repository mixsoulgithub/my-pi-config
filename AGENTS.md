# Global AGENTS.md

## Output Rules
1. **No emoji** in any output — code, comments, commit messages, file contents. Use pure ASCII. Chinese characters are allowed. Use `[OK]`, `[!]`, `[X]` etc. instead of emoji symbols.

## Q&A Recording
1. Record user questions and your answers simply in corresponding `*/doc/*.md` files. Do NOT do this when in `note/` or `work_note/` projects.
